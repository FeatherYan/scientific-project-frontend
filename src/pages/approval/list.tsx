import { PagePlaceholder } from '../../components/common/PagePlaceholder'

export default function ApprovalListPage() {
  return (
    <PagePlaceholder
      title="项目审批列表"
      description="这里会承接管理员端的核心业务闭环，后续接入搜索、表格、状态筛选和审批入口。"
      tags={['管理员', '列表页', '待审批']}
    />
  )
}
