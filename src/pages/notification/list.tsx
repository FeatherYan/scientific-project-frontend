import { PagePlaceholder } from '../../components/common/PagePlaceholder'

export default function NotificationListPage() {
  return (
    <PagePlaceholder
      title="通知列表"
      description="增强模块第一版先确定路由与菜单入口，后续这里会补消息状态、分页和筛选。"
      tags={['增强模块', '通知中心', '列表页']}
    />
  )
}
