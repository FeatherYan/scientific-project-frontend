import { PagePlaceholder } from '../../components/common/PagePlaceholder'

export default function ProjectFormPage() {
  return (
    <PagePlaceholder
      title="项目申报表单"
      description="这里会承接新建/编辑复用表单。当前业务语义调整后，它表示用户在‘可申报项目’里发起申报后，对草稿进行补充和编辑。"
      tags={['表单页', '新建/编辑复用', 'Ant Design Form']}
    />
  )
}
