import { PagePlaceholder } from '../../components/common/PagePlaceholder'

export default function ProjectListPage() {
  return (
    <PagePlaceholder
      title="项目申报列表"
      description="普通用户端的入口页面，后续会补搜索区、表格区、状态标签和操作按钮。"
      tags={['普通用户', '列表页', '状态流转']}
    />
  )
}
